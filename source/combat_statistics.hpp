#pragma once

#include <vector>

#include "log_parser.hpp"

class CombatStatistics {
  public:
    CombatStatistics(uint64_t combat_start_ms, const std::vector<LogParserTypes::Actor>& actors) {
    }
};
